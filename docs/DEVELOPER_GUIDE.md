# Developer Guide

---

## Section 1 — What This Application Does

### 1.1 Purpose

This system calculates and manages sales incentives for insurance agents. It receives policy transaction data from **Life Asia AS400** (via SFTP) and **KGILS Penta** (via REST API), applies configurable incentive rules, calculates each agent's earnings including MLM hierarchy overrides, routes results through an approval workflow, and generates payment files for **SAP FICO** or **Oracle Financials**.

### 1.2 Who Uses This System

| Role | What They Do |
|------|-------------|
| **Admin / Plan Manager** | Create incentive programs, define KPI rules, configure incentive rates and payout slabs |
| **Operations** | Upload CSV data, monitor integration pipelines, trigger calculation runs |
| **Finance** | Review approved payouts, generate SAP FICO or Oracle Financials payment files |
| **Agent / Manager** | View own incentive breakdown, check leaderboard standings, track team performance |

### 1.3 Key Business Concepts

**INCENTIVE PROGRAM**
A time-bound scheme that defines how agents are rewarded for selling insurance products. Each program groups together its own KPI rules, payout slabs, and eligibility criteria. Multiple programs can run concurrently for different product lines or agent tiers.

**KPI (Key Performance Indicator)**
A measurable metric used to evaluate an agent's performance within a program — for example, total FYP collected, policy count, or persistency ratio. KPI definitions include target thresholds that determine whether an agent qualifies for incentive payouts.

**MILESTONE**
A checkpoint within a KPI that unlocks a specific reward tier when the agent reaches it. Milestones are cumulative; hitting a higher milestone includes the benefit of all lower ones. They drive gamification elements such as leaderboard badges.

**PAYOUT SLAB**
A range-based rate table that maps KPI achievement levels to incentive amounts or percentages. For example, 80–100% achievement may pay 5% of FYP while 100–120% pays 8%. Slabs support both flat-amount and percentage-based payouts.

**PERSISTENCY GATE**
A qualifying condition that checks whether an agent's previously sold policies are still active (not lapsed). If the persistency ratio falls below the configured threshold, the agent is disqualified from the incentive payout for that period regardless of other KPI achievements.

**MLM OVERRIDE**
A multi-level marketing commission structure where a manager earns a percentage override on the incentive payouts of agents in their downline hierarchy. Override percentages are configurable per hierarchy level and program. The hierarchy tree is synced from an external Hierarchy System.

**CLAWBACK**
A reversal mechanism that recovers previously paid incentives when the underlying policy is cancelled or lapses within a defined window (typically the first policy year). Clawback amounts are deducted from the agent's future payouts automatically.

**POLICY YEAR**
The annual period starting from a policy's issue date. Incentive calculations and persistency checks are anchored to the policy year. First-year and renewal-year policies typically carry different incentive rates.

**FYP (First Year Premium)**
The total premium collected during the first policy year. FYP is the most common base metric for incentive calculations in life insurance. It reflects new business value generated by the agent.

**APE (Annual Premium Equivalent)**
A standardized measure that converts single-premium and regular-premium policies to a comparable annual figure. It is calculated as the full annual premium plus 10% of any single premium. APE is used for fair cross-product KPI comparison.

**NB (New Business)**
Policies that are newly issued within the incentive period. NB count and NB premium are common KPI metrics. Only policies with status "Issued" or "In-Force" within the evaluation window qualify as new business.

---

## Section 2 — Application Architecture

### 2.1 Technology Stack

| Layer | Technology | Version |
|-------|-----------|---------|
| Frontend | React + Vite | 18.x / 8.x |
| Styling | TailwindCSS | 4.x |
| Charts | Recharts | 3.x |
| State Management | Redux Toolkit + RTK Query | 2.x |
| Backend | Node.js + Express | 20.x / 4.x |
| Database | PostgreSQL | 15+ |
| DB Driver | node-postgres (pg) — raw SQL, **no ORM** | 8.x |
| File Upload | Multer | 2.x |
| CSV (server) | csv-parse | 6.x |
| CSV (client) | PapaParse | 5.x |
| Auth | jsonwebtoken | 9.x |
| SFTP | ssh2-sftp-client | 12.x |
| Scheduler | node-cron | 4.x |
| API Docs | swagger-ui-express (5.x) + swagger-jsdoc (6.x) | — |
| Excel Export | ExcelJS | 4.x |

### 2.2 Folder Structure

```
/
├── package.json                              # Root workspace — dev script runs client + server
├── client/
│   └── src/
│       ├── main.jsx                          # React entry point
│       ├── App.jsx                           # Route definitions and layout
│       ├── components/
│       │   └── AppShell.jsx                  # Sidebar navigation and page wrapper
│       ├── store/
│       │   ├── index.js                      # Redux store configuration
│       │   └── apiSlice.js                   # RTK Query API slice for all endpoints
│       └── pages/
│           ├── Dashboard.jsx                 # Home dashboard with summary cards
│           ├── AdminPlanListing.jsx           # List / search / activate incentive programs
│           ├── CreatePlan/
│           │   ├── index.jsx                 # Multi-step plan creation wizard container
│           │   ├── Step1_PlanDetails.jsx     # Program name, dates, product selection
│           │   ├── Step3_UserGroups.jsx      # Agent group assignment
│           │   ├── Step4_KPIRules.jsx        # KPI definitions and thresholds
│           │   └── Step5_PayoutRules.jsx     # Payout slab and rate configuration
│           ├── DataManagement/
│           │   ├── UploadCenter.jsx          # Unified CSV upload hub
│           │   ├── UploadPolicyTransactions.jsx  # Policy transaction CSV upload
│           │   ├── UploadAgents.jsx          # Agent master CSV upload
│           │   ├── UploadProducts.jsx        # Product master CSV upload
│           │   ├── UploadPerformance.jsx     # Performance data CSV upload
│           │   ├── UploadPersistencyData.jsx # Persistency data CSV upload
│           │   ├── UploadIncentiveRates.jsx  # Incentive rate CSV upload
│           │   └── DerivedVariables.jsx      # Manage derived/calculated variables
│           ├── IncentiveBreakdown.jsx        # Agent-level incentive detail view
│           ├── Leaderboard.jsx               # Agent ranking and gamification board
│           ├── PayoutDisbursement.jsx        # Finance approval and payment file generation
│           ├── TeamPerformance.jsx           # Manager view of downline performance
│           └── Integration/
│               └── IntegrationDashboard.jsx  # SFTP/API status, file logs, manual triggers
│
├── server/
│   ├── package.json                          # Server dependencies (Express, pg, etc.)
│   ├── index.js                              # Express app bootstrap, route mounting, cron start
│   └── src/
│       ├── config/
│       │   └── swagger.js                    # Swagger/OpenAPI auto-generation config
│       ├── routes/
│       │   ├── auth/
│       │   │   └── systemToken.js            # System-to-system JWT token issuance
│       │   ├── programs.js                   # CRUD for incentive programs
│       │   ├── kpis.js                       # KPI rule definitions and thresholds
│       │   ├── groups.js                     # Agent group management
│       │   ├── products.js                   # Product master lookups
│       │   ├── agents.js                     # Agent master lookups
│       │   ├── incentiveRates.js             # Rate table CRUD
│       │   ├── derivedVariables.js           # Derived variable definitions
│       │   ├── persistencyData.js            # Persistency ratio data
│       │   ├── policyTransactions.js         # Policy transaction queries
│       │   ├── performance.js                # Performance data queries
│       │   ├── upload.js                     # CSV file upload and parsing
│       │   ├── calculate.js                  # Trigger incentive calculation runs
│       │   ├── incentiveResults.js           # Query calculated incentive results
│       │   ├── payouts.js                    # Payout approval workflow
│       │   ├── leaderboard.js                # Leaderboard data endpoint
│       │   ├── dashboard.js                  # Dashboard summary endpoint
│       │   └── integration/
│       │       ├── penta.js                  # Inbound KGILS Penta REST webhook
│       │       ├── lifeasia.js               # Inbound Life Asia event webhooks
│       │       ├── export.js                 # Outbound SAP FICO / Oracle AP file generation
│       │       └── status.js                 # Integration status, logs, manual triggers
│       ├── engine/
│       │   ├── insuranceCalcEngine.js        # Core incentive calculation engine
│       │   └── calculateIncentive.js         # Orchestrator for calculation runs
│       ├── jobs/
│       │   ├── sftpPoller.js                 # Scheduled SFTP polling from Life Asia AS400
│       │   └── hierarchySync.js              # Scheduled hierarchy tree sync via REST
│       ├── middleware/
│       │   ├── systemAuth.js                 # JWT auth for system-to-system API calls
│       │   ├── userAuth.js                   # User authentication middleware (placeholder)
│       │   └── maskResponse.js               # Policy number masking in API responses
│       ├── utils/
│       │   ├── csvParser.js                  # CSV parsing and validation helpers
│       │   ├── bulkInsert.js                 # Batch INSERT / COPY for large datasets
│       │   ├── errorCodes.js                 # Standardized error codes and apiError helper
│       │   └── dataMask.js                   # Policy number masking utilities
│       └── db/
│           ├── pool.js                       # PostgreSQL connection pool and query helper
│           ├── queryHelper.js                # Dynamic query builder utilities
│           ├── migrations/
│           │   ├── 001_master_schema.sql     # Core tables (programs, KPIs, agents, etc.)
│           │   ├── 002_insurance_schema.sql  # Insurance-specific tables
│           │   ├── 002_add_team_override_pct.sql  # Add team override percentage column
│           │   ├── 003_integration_schema.sql     # Integration and staging tables
│           │   ├── 003_payout_disbursement_log.sql # Payout disbursement audit log
│           │   ├── 004_staging_tables.sql    # Staging tables for inbound data
│           │   └── 005_outbound_file_log.sql # Outbound payment file tracking
│           └── functions/
│               └── compute_agent_kpi.sql     # PL/pgSQL function for KPI computation
│
└── docs/
    ├── COPILOT_CONTEXT.md                    # AI assistant context document
    ├── DATABASE.md                           # Database schema reference
    ├── SCHEMA_GUIDE.md                       # Schema conventions and guidelines
    ├── INTEGRATION_ARCHITECTURE.md           # Integration design document
    ├── DEVELOPER_GUIDE.md                    # This file
    └── api/
        ├── README.md                         # API documentation index
        ├── API_REFERENCE.md                  # Endpoint reference guide
        ├── ERROR_CODES.md                    # Error code catalog
        ├── CHANGELOG.md                      # API versioning changelog
        ├── POSTMAN_GUIDE.md                  # Postman collection usage guide
        ├── INBOUND_PENTA_API.md              # KGILS Penta inbound spec
        ├── INBOUND_LIFEASIA_FILE.md          # Life Asia SFTP file spec
        ├── INBOUND_HIERARCHY_API.md          # Hierarchy API inbound spec
        ├── OUTBOUND_SAP_FICO.md              # SAP FICO payment file spec
        ├── OUTBOUND_ORACLE_FINANCIALS.md     # Oracle Financials payment file spec
        ├── openapi.yaml                      # OpenAPI 3.0 specification
        ├── IncentiveSystem.postman_collection.json  # Postman collection
        └── environments/                     # Postman environment files
```

### 2.3 Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         EXTERNAL SOURCE SYSTEMS                             │
└─────────────────────────────────────────────────────────────────────────────┘

  ┌──────────────────┐   ┌──────────────────┐   ┌──────────────────────────┐
  │  Life Asia AS400  │   │   KGILS Penta    │   │    Hierarchy System      │
  │    (SFTP Server)  │   │   (REST API)     │   │      (REST API)          │
  └────────┬─────────┘   └────────┬─────────┘   └────────────┬─────────────┘
           │                      │                           │
           │ SFTP pull            │ REST push                 │ REST pull
           │ (cron: nightly)      │ (webhook)                 │ (cron: nightly)
           ▼                      ▼                           ▼
  ┌──────────────────┐   ┌──────────────────┐   ┌──────────────────────────┐
  │  sftpPoller.js   │   │ integration/     │   │   hierarchySync.js       │
  │  (node-cron +    │   │ penta.js         │   │   (node-cron + fetch)    │
  │   ssh2-sftp)     │   │                  │   │                          │
  └────────┬─────────┘   └────────┬─────────┘   └────────────┬─────────────┘
           │                      │                           │
           ▼                      │                           ▼
  ┌──────────────────┐            │              ┌──────────────────────────┐
  │  stg_policy_     │            │              │  ins_agents              │
  │  transactions    │            │              │  (agent master +         │
  │  stg_agent_      │            │              │   hierarchy tree)        │
  │  master          │            │              └────────────┬─────────────┘
  │  (staging)       │            │                           │
  └────────┬─────────┘            │                           │
           │ validate & promote   │                           │
           ▼                      ▼                           │
  ┌──────────────────────────────────────────┐                │
  │  ins_policy_transactions                 │                │
  │  ins_products                            │                │
  │  ins_performance_data                    │                │
  │  ins_persistency_data                    │                │
  │  (production tables)                     │◄───────────────┘
  └────────────────────┬─────────────────────┘
                       │
                       ▼
  ┌──────────────────────────────────────────┐
  │       insuranceCalcEngine.js             │
  │  ┌────────────────────────────────────┐  │
  │  │ 1. Load program + KPI rules       │  │
  │  │ 2. Evaluate agent KPIs            │  │
  │  │ 3. Apply persistency gate         │  │
  │  │ 4. Match payout slabs             │  │
  │  │ 5. Calculate base incentive       │  │
  │  │ 6. Apply MLM hierarchy overrides  │  │
  │  │ 7. Apply clawback adjustments     │  │
  │  └────────────────────────────────────┘  │
  └────────────────────┬─────────────────────┘
                       │
                       ▼
  ┌──────────────────────────────────────────┐
  │  ins_incentive_results                   │
  │  (calculated payouts per agent)          │
  └────────────────────┬─────────────────────┘
                       │
                       │  Approval Workflow
                       ▼
  ┌──────────────────────────────────────────┐
  │  integration/export.js                   │
  │  (generate payment files)                │
  └──────────┬──────────────────┬────────────┘
             │                  │
             ▼                  ▼
  ┌─────────────────┐  ┌─────────────────────┐
  │  SAP FICO CSV   │  │  Oracle Financials   │
  │  Payment File   │  │  AP CSV File         │
  └─────────────────┘  └─────────────────────┘
```

<!-- Sections 3-10 will be appended next -->
